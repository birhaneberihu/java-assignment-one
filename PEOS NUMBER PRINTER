 
package posenumberpointer;
import java.math.*;
import java.util.Scanner;

/**
 *
 * @author birhane
 */
public class PoseNumberpointer {
static Scanner birhane = new Scanner(System.in);
	static Math m;
    
	
	static int maximum(int list[]){
       	int maximum =list[0];
         for(int u=1;i<list.length-1;u++){
        	 if(list[u]>maximum)
        		 maximum = list[u];
         }	  
         return maximum;
    }
	
    static void  insertList(int list[],int c){
	 			
		System.out.println("\nENTER THE NUMBERS.\n");
	 	
		for(int u=0;u<c;u++){
	 		System.out.print("PLEASE ENTER NUMBER"+(u+1)+":");
	 	    list[u]=birhane.nextInt();
		}
	}
	
    
    static void display(int list[],int c){
    
    
    	System.out.print("\n the Even Numbers is/are= ");
    	for(int u=0;u<c;u++){
    		if(list[u]%2 == 0)
    		 System.out.print(list[u]+" ");
    	}
    	
    	
    	System.out.print("\nthe Odd Number is/are= ");
    	for(int u=0;u<c;u++){
    		if(list[u]%2 !=0)
    		 System.out.print(list[u]+" ");
    	}
    
   	
    	System.out.print("\nPrime Numbers: ");
    	
       	for(int u=0;u<c;u++){
    		if(list[u]%2!=0 && list[u]%3!=0 && list[u]%5!=0 && list[u]%7 !=0)
    		    System.out.print(list[u]+" ");
    		  }
   
    	System.out.print("\n  Large= ");
    	int Large =list[0];
    	for(int u=0;u<c;u++){
    		if(list[u] > Large)
    		  Large = list[u];
    	}
    	System.out.println(Large);

  
    	System.out.print(" Smalle=: ");
    	int smalle = list[0];
    	for(int u=0;u<c;u++){
    		if(list[u] < smalle)
    		  smalle= list[u];
    	}
    	System.out.println(smalle);
    	
    }
	public static void main(String[] args) {
		
       int r;
       
       System.out.print("Enter the size of the list: ");	  	
	   r = birhane.nextInt();
	   
	   int listOne[] = new int[r];
	   
	   insertList(listOne,r);
	   display(listOne,r);
	}

}

   
   
    

